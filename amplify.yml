version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm run install:clean
    build:
      commands:
        - npm run build:prod
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
